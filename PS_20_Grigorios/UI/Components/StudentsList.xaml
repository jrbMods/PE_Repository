<UserControl x:Class="UI.Components.StudentsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- DataGrid to display students with custom column definitions -->
        <DataGrid x:Name="students" ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <!-- Column for Id -->
                <DataGridTextColumn Header="#" IsReadOnly="True" Binding="{Binding Id}" />

                <!-- Column for Names -->
                <DataGridTextColumn Header="Names" IsReadOnly="True" Binding="{Binding Names}" />

                <!-- Column for Role -->
                <DataGridTextColumn Header="Roles" IsReadOnly="True" Binding="{Binding Role}" />

                <!-- Column for Expires In -->
                <DataGridTextColumn Header="Expires In" IsReadOnly="True" Binding="{Binding Expires}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>